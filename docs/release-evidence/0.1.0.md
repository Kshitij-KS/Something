---
meta:
  title: Assess Callback 0.1.0 candidate evidence
  navLabel: 0.1.0 Evidence
  category: Project
  contentType: Reference
  plan: ../documentation-plan.md
---

# Assess Callback 0.1.0 candidate evidence

This record captures the available evidence for the local `0.1.0` artifacts. It concludes that they are unsigned engineering-test candidates, not a qualified public release.

## Record the candidate decision

| Field                       | Result                                                                                   |
| --------------------------- | ---------------------------------------------------------------------------------------- |
| Version                     | `0.1.0`                                                                                  |
| Decision                    | Retain as an engineering-test candidate                                                  |
| Public-release approval     | Not granted                                                                              |
| Source provenance           | Local working tree with uncommitted implementation changes                               |
| Reproducible named revision | Not established                                                                          |
| Evidence timestamp          | Not recorded for the original qualification run                                          |
| Human gates                 | All pending by repository default                                                        |
| Installed Windows matrix    | Incomplete and human-owned                                                               |
| Signature                   | Nullsoft Scriptable Install System (NSIS) and Windows Installer (MSI) report `NotSigned` |

No named commit identifies the source that produced these artifacts. Their hashes identify the generated bytes, but they cannot establish a clean source revision.

## Record an unattributed historical validation report

An earlier local run reported the following results. No retained timestamp, transcript, workflow identity, or clean source revision ties them to these artifacts, so they are not candidate qualification evidence:

- 116 runnable Rust tests passed
- One private-corpus Rust test intentionally ignored
- 14 of 14 extension tests passed
- Rust formatting passed
- Locked workspace release check passed
- Locked workspace release tests passed with one Windows job
- Release Clippy across all targets passed with warnings denied
- `callback-app` no-default-features release check passed
- TypeScript checking passed
- ESLint passed with zero warnings
- Prettier check passed
- Production app and extension builds passed
- Extension validation passed
- Static local-only audit passed
- Git diff whitespace validation passed

These results do not cover live browser markup or installed Windows behavior.

## Record completed reviews

The implementation review outcomes were:

- `RACE REVIEW: PASS`
- `UI REVIEW: PASS`
- `APPROVED`

Review approval covers the inspected code change. It does not close human gates or release certification.

## Record artifact bytes

This record does not retain the hash-validation timestamp. The documented artifact evidence is:

| File                           |     Bytes | SHA-256                                                            |
| ------------------------------ | --------: | ------------------------------------------------------------------ |
| `Callback_0.1.0_x64-setup.exe` | 3,477,777 | `c2a4939c16dc2304e54f3c682f7a33b3f5fae61b396939d7274da23caa1f7fce` |
| `Callback_0.1.0_x64_en-US.msi` | 5,173,248 | `43ef1b56246ffa850ee686dbb6729e2b487970f88e7412e840b0a81aa5cb83de` |
| `callback-extension-0.1.0.zip` |     6,575 | `b024e839ccad44d1c287b6543b044af9f434862bcea5899a8581d77c459d24dd` |
| `SHA256SUMS.txt`               |       288 | Manifest, not a payload entry                                      |

The three payload hashes match `artifacts/SHA256SUMS.txt`.

Packaging inspection also recorded:

- The extension ZIP contains exactly `manifest.json`, `background.js`, `content.js`, and `selectors.json`
- NSIS and MSI contain `callback-native-host.exe`
- Protocol commands quote the executable and `%1`
- Installer payloads match their Tauri bundle outputs

## Record signature state

Authenticode inspection recorded:

| File                           | Status      |
| ------------------------------ | ----------- |
| `Callback_0.1.0_x64-setup.exe` | `NotSigned` |
| `Callback_0.1.0_x64_en-US.msi` | `NotSigned` |

SmartScreen and Mark-of-the-Web warnings are expected. SHA-256 values are not publisher signatures.

## Keep human evidence pending

| Gate                       | Status         | Reason                                        |
| -------------------------- | -------------- | --------------------------------------------- |
| `phase0_five_day`          | `pending_user` | Five-day installed use is not recorded        |
| `extraction_precision_300` | `pending_user` | Private 300-message aggregate is not recorded |
| `acceptance_two_week`      | `pending_user` | Two-week trial and formula are not recorded   |

Do not mark these gates passed from automated tests, source reviews, or artifact generation.

## Keep installed qualification pending

The following remain outside this record:

- Separate NSIS and MSI clean-machine installation
- Warm and stopped-app notification actions
- Windows Focus Assist, lock, sleep, resume, and real logon autostart
- Live Gmail and Slack canaries
- Native-host registration across install ordering and path changes
- Retention, purge, update, and uninstall matrix
- Installed-process Transmission Control Protocol (TCP) and User Datagram Protocol (UDP) endpoint inspection
- General accessibility matrix

Follow [`../release.md`](../release.md) before changing this candidate's decision.
